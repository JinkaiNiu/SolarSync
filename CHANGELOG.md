# 更新日志

## v1.0.3.0 (2026-05-26)

### 修复

- **修复跨日后主题切换失效的问题**
  - `ScheduleNextDailyCheck()` 只在初始化时调用一次，每日定时器首次触发后不再续约，导致 `CurrentSolarInfo` 跨日后数据陈旧、`TimeUntilSunset` 返回值异常、定时器坠入1分钟轮询死循环
  - 修复：`RefreshAsync()` 末尾增加 `ScheduleNextDailyCheck()`，每次数据刷新后重新设定每日定时器
- **修复边界时刻定时器精度问题**
  - `ScheduleNextSwitch()` 改用 `TimeOfDay` 相对今日日期计算（而非依赖原始 `DateTime` 中过期的日期部分），消除陈旧数据导致的负延迟
  - 最小延迟设为 1 秒，防止亚毫秒截断为 0 导致定时器立即重入

### 新增

- 新增一致性校验定时器 `_syncTimer`（60 秒间隔），作为精确定时器的安全兜底
  - 定期检查当前 Windows 主题是否与当前时段应处状态一致，若不匹配自动修正
  - 即使精确定时器因边界条件错过切换点，也能在 60 秒内修正

### 修改

- 版本号更新为 1.0.3.0
- `PROJECT.md` 新增定时器安全兜底设计章节

---

## v1.0.2.0 (2026-05-26)

### 修复

- **修复"到达时间点后不会自动切换"的问题**
  - `ScheduleNextSwitch()` 夜间分支改为精确计算到次日日出的时间，消除每小时轮询
- **修复"程序自动退出"的问题**
  - `PerformScheduledSwitch()` 中 `TaskScheduler.FromCurrentSynchronizationContext()` 在 ThreadPool 线程上抛出异常导致进程崩溃，改用 `TaskScheduler.Default`
  - 给定时器回调包裹 `try-catch` 防止其他未捕获异常导致退出

### 修改

- 版本号更新为 1.0.2.0

---

## v1.0.1.0 (2026-05-25)

### 新增

- 启动时自动应用一次当前时段对应的 Windows 主题模式
  - 程序启动后，根据日出日落计算结果，立即将系统主题设为浅色或深色
  - 无需等到下一次日出/日落定时器触发才开始工作

### 修改

- 版本号更新为 1.0.1.0
- `AppStateManager.InitializeAsync()` 增加启动时主题自动应用逻辑

---

## v1.0.0.0 (2026-05)

### 首次发布

- 自动根据日出日落时间切换 Windows 浅色/深色模式
- 基于 myip.ipip.net 的 IP 自动定位（国内城市）
- 内置 350+ 中国城市经纬度坐标数据库
- NOAA 太阳位置算法精确计算日出日落
- 系统托盘运行，支持开机自启
- 支持手动强制切换浅色/深色模式
- 时间线可视化面板展示 24 小时昼夜分布
- IP 定位结果本地缓存，支持离线使用
- 单文件自包含发布，无需 .NET 运行时
